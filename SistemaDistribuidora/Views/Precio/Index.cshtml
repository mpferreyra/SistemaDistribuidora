@model IEnumerable<SistemaDistribuidora.Models.PrecioModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaAlta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaBaja)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaAlta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaBaja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrecioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrecioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrecioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
